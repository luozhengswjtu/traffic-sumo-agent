# TrafficAgent MVP 计划

## 1. 项目目标

`TrafficAgent` 的第一阶段目标，是做成一个基于 `PySide6` 的单体桌面应用，让用户可以通过聊天和基础参数面板完成以下闭环：

1. 新建一个交通仿真项目
2. 用自然语言生成基础 SUMO 场景
3. 自动生成并保存可运行的 SUMO 文件
4. 在界面中调整少量关键参数
5. 直接启动一次仿真并看到过程与状态

项目定位不是“聊天机器人套壳”，而是“面向交通仿真的桌面 Agent 工具”。

## 2. MVP 成功标准

MVP 是否成立，以以下 5 条为准：

1. 用户可以新建、打开、切换仿真项目
2. 用户可以通过自然语言生成基础场景
3. 应用可以稳定生成一套可运行的 SUMO 文件
4. 用户可以通过界面调整基础参数后重新运行仿真
5. 仿真状态、日志和基础指标可以在界面中查看

## 3. MVP 范围

### 3.1 必须完成

- 项目管理
  - 新建项目
  - 打开项目
  - 最近项目列表
- 聊天式指令入口
  - 输入场景需求
  - 返回执行摘要
- 基础场景模板
  - 十字路口
  - T 字路口
  - 直线路段
- SUMO 文件生成
  - `.nod.xml`
  - `.edg.xml`
  - `.rou.xml`
  - `.sumocfg`
  - `.net.xml`
- 参数面板
  - 仿真总时长
  - 仿真步长
  - 流量等级
  - 随机种子
  - 路口类型
- 一键运行仿真
- 仿真控制
  - 开始
  - 暂停
  - 停止
  - 重跑
- 仿真状态展示
  - 当前时间步
  - 运行状态
  - 当前车辆数
  - 日志输出
- 模型 API 配置
  - `base_url`
  - `api_key`
  - `model`
- 基础偏好设置
  - 默认场景类型
  - 默认流量等级
  - 默认仿真时长
- 基础校验
  - 文件完整性检查
  - 关键参数检查
  - SUMO 启动前校验

### 3.2 最好完成

- 聊天和参数面板双向联动
- 项目内操作历史
- 执行结果摘要
- 场景复制与快速重跑
- 仿真结束后的基础统计摘要

### 3.3 暂不纳入 MVP

- 任意复杂城市级路网自由生成
- 多 Agent 协作
- 高级可视化路网编辑器
- 复杂信控方案编辑器
- 云端同步
- 批量实验调度
- 精细化微观驾驶行为建模

## 4. 技术路线

### 4.1 技术栈

- 语言：`Python 3.11+`
- 桌面框架：`PySide6`
- 领域建模：`pydantic`
- SUMO 集成：`sumo` / `traci` / `libsumo`
- XML 处理：`lxml`
- 本地存储：`SQLite + JSON`
- 模型接入：`OpenAI 兼容 API`

### 4.2 架构原则

1. 大模型不直接作为 XML 生成器
2. 大模型只负责理解需求、补全参数和规划工具调用
3. 文件生成必须经过领域模型和确定性代码
4. 仿真运行和 UI 展示解耦
5. 参数面板和聊天输入共同驱动同一份项目状态

## 5. 核心链路

主链路固定为：

`用户输入 -> Agent 解析意图 -> 生成结构化计划 -> 工具生成/修改 SUMO 文件 -> 校验 -> 启动仿真 -> UI 回显状态`

MVP 只要把这条链路做稳，产品就成立。

## 6. 里程碑计划

### M1：项目骨架与基础 UI

目标：应用可启动，项目可创建，设置可保存。

交付：

- PySide6 主窗口
- 聊天区、参数区、日志区基础布局
- 项目工作区管理
- 配置读写
- 模型设置页雏形
- 偏好设置页雏形

验收标准：

- 应用可启动
- 可创建项目目录
- 基础设置可保存和加载
- 主界面可正常交互

建议周期：`3-4 天`

### M2：SUMO 文件生成链路

目标：从自然语言到可运行文件。

交付：

- 基础领域模型
- 模板化网络生成器
- 路由/流量生成器
- `sumocfg` 生成器
- `net.xml` 生成流程
- 基础校验器
- Agent 第一版工具调用链路

验收标准：

- 三类基础场景都能生成
- 文件结构完整
- 正常输入可以生成可运行配置
- 失败场景有明确错误提示

建议周期：`5-7 天`

### M3：仿真运行与界面联动

目标：生成后可直接运行，形成闭环。

交付：

- 仿真运行器
- 开始、暂停、停止、重跑控制
- 运行状态回传 UI
- 参数面板与项目联动
- 基础指标展示
- 日志与执行摘要

验收标准：

- 一键运行当前项目
- 支持暂停/停止/重跑
- 支持修改参数后再次运行
- 运行状态和基础指标可见

建议周期：`4-6 天`

### M4：可用性打磨

目标：可演示、可试用、可继续迭代。

交付：

- 指令模板优化
- 错误提示优化
- 最近项目和历史记录
- 基础偏好注入
- 模型配置完整化
- 稳定性修复
- 启动说明与打包说明

验收标准：

- 非开发者可完成主流程
- 常见错误不会直接导致应用不可用
- 可重复创建、修改、运行多个项目

建议周期：`3-5 天`

## 7. MVP 验收用例

至少通过以下 5 条：

1. “生成一个双向四车道十字路口，仿真 1200 秒。”
2. “生成一个 T 字路口，主路流量更高。”
3. “把当前场景流量提高 30%，重新运行。”
4. “把仿真步长改成 0.5。”
5. “切换模型 API 配置后继续生成场景。”

## 8. 风险与控制

### 8.1 主要风险

- 直接让模型自由写 XML，导致稳定性差
- 一开始把范围做太大，拖慢 MVP 落地
- 过早追求复杂仿真可视化
- 缺少结构化中间层，后续维护困难

### 8.2 控制策略

- 所有文件生成走工具层
- 先做模板化场景
- 先跑通状态和闭环，再优化视觉表现
- 用领域对象统一 UI、Agent、工具层的输入输出

## 9. 下一步实施建议

建议立刻按以下顺序推进：

1. 建立项目骨架和目录结构
2. 完成主窗口、项目管理和设置存储
3. 建立领域模型与 SUMO 生成器
4. 接入 Agent 编排器
5. 接入仿真运行器
6. 完成主流程联调和验收
