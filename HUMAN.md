# HUMAN 说明

这份文档是写给你看的，尽量不讲太多“框架黑话”，主要帮助你快速知道：

- 现在已经做了什么
- 这些东西放在哪里
- 下一步该接着做什么
- 有哪些地方先不要误会

## 1. 目前已经完成了什么

现在已经完成的是 `Phase A`，也就是“项目底座”。

通俗一点说，就是先把房子的地基和总线搭起来了，还没有开始装修客厅、厨房和卧室。

已经完成的内容有：

- 建好了基础目录
  - `app/`
  - `storage/`
  - `sumo_domain/`
  - `workspace/projects/`

- 建好了应用入口
  - `app/main.py`

- 建好了应用装配器
  - `app/bootstrap.py`

- 建好了本地配置存储
  - `storage/config_store.py`

- 建好了项目存储
  - `storage/project_store.py`

- 建好了最近项目记录
  - `storage/recent_store.py`

- 建好了一个预留数据库
  - `storage/db.py`

- 建好了核心数据模型
  - `sumo_domain/project_spec.py`
  - `sumo_domain/network_spec.py`
  - `sumo_domain/route_spec.py`
  - `sumo_domain/simulation_spec.py`
  - `sumo_domain/preferences.py`

## 2. 这些文件分别是干什么的

### `app/main.py`

这是程序入口。

以后真正启动桌面应用，就是从这里进。  
当前它会尝试启动一个非常简单的 `PySide6` 窗口，用来证明底层初始化已经打通。

### `app/bootstrap.py`

这是“装配中心”。

它的作用是把应用运行时需要的基础对象都准备好，比如：

- 配置存储
- 项目存储
- 最近项目记录
- 本地路径约定

以后 UI、Agent、仿真运行器这些模块，也会从这里拿公共依赖。

### `storage/`

这是“本地保存数据”的地方。

现在负责：

- 保存应用配置
- 保存模型配置
- 保存用户偏好
- 创建和读取项目目录
- 记录最近打开的项目

### `sumo_domain/`

这是“核心数据结构定义”的地方。

后面整个项目都应该围绕这里定义的数据结构来开发，而不是到处直接拼 XML 字符串。

你可以简单理解成：

- `ProjectMeta`：一个项目的基本信息
- `NetworkSpec`：路网结构
- `RouteSpec`：流量和路线
- `SimulationSpec`：仿真参数
- `UserPreferences`：用户偏好
- `ModelConfig`：模型 API 设置

## 3. 现在还没有完成什么

这一点很重要。

虽然骨架已经有了，但下面这些还没做：

- 还没有正式的主界面
- 还没有聊天面板
- 还没有参数面板
- 还没有 SUMO 文件生成器
- 还没有仿真运行器
- 还没有 Agent 编排器
- 还没有模型 API 真正接入

所以你现在可以把它理解成：

“项目结构已经立起来了，但功能还没有进入可用阶段。”

## 4. 目前目录里最重要的几个位置

- 计划文档：[plan.md](E:\sumoAndQt\TrafficAgent\plan.md)
- 任务拆分：[route.md](E:\sumoAndQt\TrafficAgent\route.md)
- 问题记录：[ChatGPT.md](E:\sumoAndQt\TrafficAgent\ChatGPT.md)

这些文档的分工可以这样记：

- `plan.md`：整体路线
- `route.md`：具体怎么拆任务
- `ChatGPT.md`：后续开发时别重复踩坑
- `HUMAN.md`：给你快速看当前进度

## 5. 下一步最合理做什么

接下来最自然的是进入 `Phase B`，也就是开始做基础 UI。

建议顺序：

1. 先做主窗口
2. 再做聊天区和参数区
3. 再做项目新建和设置对话框

等 UI 基础能看见之后，再进入：

- `Phase C`：SUMO 文件生成
- `Phase D`：仿真运行
- `Phase E`：模型接入和 Agent 编排

## 6. 目前你需要注意的地方

### 依赖可能还没装

当前代码已经按目标方向写了，但像下面这些依赖还不一定已经装好：

- `PySide6`
- `pydantic`

如果你现在直接运行，可能会因为缺依赖报错。

### 现在还不是可演示版本

当前完成的是“基础工程”，不是“可跑完整仿真的桌面产品”。

如果后面继续开发，应该按阶段走，不要一下子跳去做很复杂的 Agent 或 UI 细节。

### 项目默认数据位置已经约定好了

以后本地数据默认会放在：

- `.traffic_agent/`

默认项目目录放在：

- `workspace/projects/`

这样后面继续扩展时，不容易把配置、项目、缓存混在一起。

## 7. 你现在可以怎么理解当前状态

如果用一句话总结：

现在已经不是“只有想法和文档”的状态了，已经进入“有真实代码骨架，可以继续一层层往上搭”的状态。

### 目前还没法直接本地运行验证

刚才自检时发现，这台机器当前没有可用的 Python 3。

这意味着：

- 代码文件已经创建好了
- 但暂时还不能在本机直接执行 Python 来检查运行效果

所以如果你下一步要继续开发，建议优先先装：

- Python 3.11+
- 后续再补 `PySide6`
- 再补 `pydantic`

## 8. Phase B 现在做到了什么

现在已经把基础界面骨架搭出来了，主要包括：

- 主窗口
- 聊天区
- 参数面板
- 仿真状态区
- 日志区
- 新建项目弹窗
- 设置弹窗

也就是说，应用现在已经从“只有底层代码结构”，进入到了“有桌面界面结构”的阶段。

不过这里要特别注意：

- 聊天区现在还没有连真实 Agent
- 参数区现在还没有连真实 SUMO 生成器
- 运行、暂停、停止现在也还没有连真实仿真运行器

所以当前的 UI 更像是“舞台已经搭好”，但演员还没有全部上场。

## 9. 接下来应该怎么接

接下来最合理的顺序还是：

1. `Phase C` 先做 SUMO 文件生成
2. `Phase D` 再接仿真运行器
3. `Phase E` 最后接模型和 Agent

这样会比先去做复杂聊天逻辑稳很多。

## 10. Phase C 现在做到了什么

现在已经把 `sumo_tools` 这一层搭起来了。

通俗一点说，就是：

- 现在不只是“有界面”
- 也开始有“生成 SUMO 文件的后端骨架”了

这一层目前已经包括：

- 路网生成器
- 路线/流量生成器
- `sumocfg` 生成器
- `netconvert` 包装器
- 项目构建器
- 基础校验器

这意味着后面如果要真正接入 Agent 或按钮逻辑，就已经有一个比较清楚的落点了，不需要再从零开始设计文件生成流程。

## 11. 现在还需要特别注意什么

### `scenario.net.xml` 目前可能只是占位文件

因为当前还没有确认这台机器装了 SUMO，也没有确认 `netconvert` 可用。

所以现在代码里做了一个现实处理：

- 如果有 `netconvert`，就走真实生成
- 如果没有，就先写一个占位版 `scenario.net.xml`

这能保证工程流程先搭起来，但它不代表“已经真的能跑 SUMO”。

### Phase C 完成的是“生成能力底座”，不是最终生成体验

现在更像是：

- 生成器类已经有了
- 输出文件的路径和格式已经约定好了
- 校验器也有了

但还没做到：

- 点击按钮就自动生成
- 聊天一句话就自动生成
- 生成完立即开始仿真

这些会分别在后面的阶段继续接上。

## 12. 下一步最合理做什么

接下来建议进入 `Phase D`。

也就是开始做：

- 仿真运行器
- 进程管理
- 状态同步
- UI 和运行状态联动

等 Phase D 完成后，再进入 Phase E 接 Agent，会更稳。

## 13. 项目环境已经配置好了

现在项目根目录里已经有一个可用的 Anaconda 环境：

- 环境路径：`E:\sumoAndQt\TrafficAgent\.conda`

这个环境里目前已经装好了：

- `Python 3.11`
- `pydantic`
- `PySide6`

### 你以后怎么用它

最直接的方式有两种。

第一种：直接调用环境里的 Python

```powershell
E:\sumoAndQt\TrafficAgent\.conda\python.exe app\main.py
```

第二种：先激活环境再运行

```powershell
conda activate E:\sumoAndQt\TrafficAgent\.conda
python app\main.py
```

如果只是想做一个快速检查，也可以这样：

```powershell
E:\sumoAndQt\TrafficAgent\.conda\python.exe -c "from app.bootstrap import AppBootstrap; print(AppBootstrap().build().paths.projects_dir)"
```

## 14. 这次环境配置里有个值得注意的坑

这次安装依赖时，`pip` 一度把你用户目录下的全局包当成“已经安装”。

但实际上，项目环境自己并不能稳定使用这些全局包，所以后来我对 `typing-extensions` 做了强制重装，才把导入链路修好。

你可以简单记住这个经验：

- 以后如果某个包 `pip` 说装了，但运行时报 `ModuleNotFoundError`
- 优先在这个项目环境里做一次 `--force-reinstall`

## 15. 当前环境验证结果

已经确认通过的检查有：

- 项目环境里的 `python` 可以执行
- `pydantic` 可以导入
- `PySide6` 可以导入
- `AppBootstrap().build()` 可以跑通并拿到项目工作区路径

这意味着现在终于可以做真正的 Python 级联调了，不再只是“写代码骨架但本机跑不起来”的状态。

## 16. Phase D 现在做到了什么

现在已经把仿真运行器接进来了。

这次不是单纯加了几个空文件，而是已经具备这些能力：

- 能找到本机的 `sumo`、`sumo-gui`、`netconvert`
- 能生成一个基础测试项目
- 能生成对应的 SUMO 文件
- 能启动一次真实的 `sumo` 进程
- 能正常结束这个进程

也就是说，当前项目已经第一次具备了“从项目目录到真实仿真进程”的链路。

## 17. 现在按钮行为是什么

当前主界面里的按钮语义是：

- `运行`
  - 如果当前项目还没有 SUMO 文件，会先自动生成一套默认场景
  - 然后启动 SUMO

- `停止`
  - 会停止当前 SUMO 进程

- `暂停`
  - 现在只是状态占位
  - 还不是真正的仿真暂停

这个区别你需要记住，因为“有按钮”不等于“控制能力已经完整”。

## 18. 为什么暂停还没做成真的

原因很直接：

- 现在是进程级控制
- 真正的暂停、继续、单步执行，需要接入 `TraCI` 或 `libsumo`

所以当前 `Phase D` 做到的是：

- 真实启动
- 真实停止
- 基础状态同步

但不是：

- 真实步进控制
- 真实暂停恢复控制

## 19. 现在项目处于什么阶段

如果一句话概括：

现在已经从“有 UI 和文件生成骨架”，进入到了“能生成项目并拉起真实仿真进程”的阶段。

这已经是一个明显的台阶。

## 20. 下一步最合理做什么

现在最合理的下一步有两条路：

1. 进入 `Phase E`
把聊天区接上真正的 Agent 编排、工具调用和模型接口。

2. 在进入 `Phase E` 前，先插一个小补强
把 `TraCI` 接入，先把暂停/继续/步进做成真的。

如果从工程顺序看，我建议：

- 先做 `Phase E` 的基础 Agent 编排
- 然后再回头补 `TraCI` 深化控制能力

因为现在最缺的是“让聊天真正驱动项目生成”，而不是继续只靠按钮和默认场景。

## 21. Phase E 现在做到了什么

现在聊天区已经不再是占位回复，而是接上了真正的 Agent 编排层。

当前已经具备的能力有：

- 聊天生成场景
- 聊天修改默认偏好
- 聊天触发运行请求
- 自动创建项目
- 自动把生成结果落盘为 SUMO 文件
- 可选直接运行仿真

也就是说，现在用户已经可以不先点很多按钮，而是直接发一句话来驱动主流程。

## 22. 当前聊天支持的典型指令

下面这些类型现在已经能走通：

- `生成一个四车道十字路口，南北向高峰流量，仿真120秒，并运行`
- `生成一个T字路口，低流量，仿真600秒`
- `把默认场景设为十字路口，默认流量设为高流量，默认时长1200秒`
- `运行一下`

当前最稳定的是这类“明确、短句、参数直给”的指令。

## 23. 当前 Agent 是怎么工作的

当前不是完全依赖大模型自由发挥，而是：

- 先用本地规则解析常见中文命令
- 如果用户配置了模型 API，并且规则没识别出来
- 再调用 OpenAI 兼容模型接口做补充解析

这样做的好处是：

- 没有 API Key 也能用
- 基础命令更稳定
- 以后还可以逐步把复杂表达交给模型补强

## 24. 当前真实验证结果

已经确认跑通的链路有：

- 聊天生成场景
- 生成 5 个基础 SUMO 文件
- 生成后直接触发运行意图
- 用 headless `sumo` 成功启动和停止
- 聊天更新默认偏好并保存

这意味着，现在项目已经进入“聊天可以真正驱动主要功能”的阶段。

## 25. 当前边界

这一步虽然已经很关键，但边界还是要看清楚：

- 复杂长文本需求还不一定解析得很好
- 暂停/继续/单步控制还不是真实 TraCI 控制
- 聊天编辑现有复杂场景的能力还比较基础

所以 Phase E 的成果，准确说是：

- 已有最小可用 Agent
- 但还不是成熟的复杂交通仿真对话系统

## 26. 下一步最合理做什么

接下来建议有两条路，优先级我给你排一下：

1. 先补强当前 Agent 的能力
重点做更完整的参数解析、项目上下文编辑、聊天修改现有场景。

2. 再补 TraCI
把暂停、继续、步进、状态读取做成真正可控。

如果按产品价值排序，我建议先做第 1 条。
因为现在最值得补强的是“聊天生成和聊天修改”的可用性，而不是先把暂停按钮做精细。

## 27. 现在聊天修改当前场景已经更像“编辑器”了

这次增强之后，聊天区不只是“重新生成一个场景”。

它现在已经能基于当前项目上下文做增量修改，也就是：

- 保留当前项目已有的参数
- 只改你在这句话里明确提到的部分

这对实际使用很关键，因为否则每一句话都等于“整场景重来”，体验会很差。

## 28. 当前已经验证通过的编辑型指令

下面这些类型现在已经确认能工作：

- `把流量提高30%，步长改成0.5，时长改成180秒`
- `把当前场景改成T字路口，3车道，限速60km/h`
- `把流量降低20%`
- `把步长改成1.5`
- `把时长改成10分钟`

这类命令的关键特征是：

- 有明确修改对象
- 参数表达直接
- 不需要太多跨句推理

## 29. 这次底层改了什么

为了让“聊天修改当前场景”稳定下来，这次加了一层显式的项目参数快照：

- `ProjectContext.scenario_state`

你可以把它理解成“当前项目的结构化设置总表”。

里面会保存类似这些内容：

- 场景类型
- 车道数
- 路段长度
- 限速
- 流量等级 / 流量值
- 偏向方向
- 时长
- 步长
- 随机种子

以后聊天修改、参数面板修改、重新构建文件，都会尽量围绕这份状态工作，而不是每次临时猜。

## 30. 现在要注意的一个行为

当前默认值是会持久保存的。

意思是：

- 如果你之前把默认流量改成了 `high`
- 下一次你只说“生成一个十字路口”
- 那它就会按 `high` 去生成

所以测试时如果你想结果完全稳定，最好把关键参数写全，不要只依赖默认值。

## 31. 当前阶段的真实结论

现在项目已经不只是“能聊天生成一次场景”，而是开始具备“聊天编辑当前项目”的能力。

这一步非常关键，因为它决定后面这个产品到底像：

- 一次性场景生成器
- 还是一个真正可迭代修改的交通仿真 Agent

当前已经开始向后者靠拢了。

## 32. 现在聊天不只会“改参数”，还会“说清当前项目是什么”

这次增强补了两个非常实际的能力：

- 你可以直接问当前项目摘要
- 你可以做取消/恢复默认这类编辑，而不只是不断叠加修改

这会让聊天更接近一个真正可维护项目状态的 Agent，而不是只能单向往前堆参数。

## 33. 当前已经支持的新增聊天句式

这轮新增并验证通过的类型包括：

- `当前项目情况`
- `当前场景摘要`
- `取消偏向`
- `取消随机种子`
- `恢复默认流量`
- `恢复默认时长`
- `恢复默认步长`
- `长度改成300米`

也就是说，现在你既可以：

- 问它“现在这个项目到底是什么状态”
- 也可以告诉它“把某些设置撤掉或恢复默认”

## 34. 一个需要理解的行为

`恢复默认流量` 不是恢复成一个固定写死的值，而是恢复成你当前保存的默认偏好值。

比如：

- 如果你之前已经把默认流量保存成 `high`
- 那么执行 `恢复默认流量`
- 结果还是会回到 `high`

所以这类命令的结果，要结合当前偏好一起理解。

## 35. 当前这一步意味着什么

如果一句话概括：

现在聊天区已经开始具备“项目状态管理器”的特征，而不只是“参数输入框”。

这一步的价值在于，后面如果继续加复杂编辑、摘要回显、变更确认、操作历史，这个 Agent 的交互会越来越像一个真正的工程工具。

## 36. 现在已经支持“变更确认 + 操作历史”

这一轮补上的核心能力有两块：

- 每次聊天改动后，Agent 会先返回一段明确的“变更确认”
- 同时会把这次操作写入本地历史库，后面可以追溯

现在你在聊天区做下面这些动作时，都会留下记录：

- 生成场景
- 修改当前场景
- 更新默认偏好
- 请求运行仿真
- 查询当前项目摘要

每条记录都会保存这些内容：

- 时间
- 项目名 / 项目目录
- 意图类型
- 用户原始消息
- 变更摘要
- 修改前状态
- 修改后状态
- 生成的文件列表
- 问题列表

## 37. 现在怎么查看历史

当前已经支持直接在聊天区输入：

- `查看最近操作历史`
- `查看最近2条操作历史`
- `最近变更`
- `历史记录`

如果当前已经有项目上下文，默认只看当前项目的历史。
如果当前没有项目上下文，就会查全局最近历史。

## 38. 这次实现的边界

这次先把“可追溯”做到了后端和聊天链路里，已经够你实际使用和排查。

但目前还没做：

- 独立的历史面板 UI
- 历史记录筛选和搜索
- 从某条历史一键回滚场景

所以当前形态更准确地说是：

- 已有持久化历史
- 已能通过聊天查询历史
- 但还不是完整的可视化历史管理器

## 39. 现在已经有独立的 GUI 历史面板

这次已经把历史查询从“只能靠聊天命令”升级成了真正的界面面板。

你现在在主窗口右侧可以看到一个新的区域：

- `操作历史`

它具备这些能力：

- 自动显示当前项目最近操作
- 点击某一条历史后，下面会显示详细内容
- 可以看到用户原始消息、变更摘要、生成文件、问题列表
- 有一个手动 `刷新` 按钮

另外菜单里也新增了：

- `视图 -> 刷新操作历史`

## 40. 这块现在的真实边界

当前 GUI 历史面板已经能满足日常追溯和排查，但它还是第一版。

已经有的：

- 独立面板
- 当前项目历史列表
- 详情查看
- 聊天执行后自动刷新并选中新记录

还没有的：

- 按意图筛选
- 全文搜索
- 历史回滚
- 导出历史

## 41. 现在暂停 / 继续 / 单步已经是真控制，不再是占位

这次已经把仿真控制从“进程级按钮”升级成了真正的 `TraCI` 控制。

现在右侧参数区的按钮含义是：

- `运行`
  - 通过 TraCI 启动 SUMO，并进入连续步进
- `暂停`
  - 真正停止继续推进 `simulationStep()`
- `继续`
  - 恢复连续步进
- `单步`
  - 只执行一次真实时间步推进
- `停止`
  - 关闭 TraCI 连接并停止 SUMO 进程

这意味着当前已经真正具备：

- 暂停后时间不再继续前进
- 单步后时间只前进一个步长
- 继续后仿真恢复推进

## 42. 这次的真实验证结果

这次不是只做了代码改动，还做了真实烟测。

验证流程是：

1. 生成一个测试场景
2. 用 headless `sumo` + TraCI 启动
3. 暂停
4. 单步
5. 继续
6. 停止

验证中看到的状态流转是：

- `paused` 时停在当前时间
- `step` 后时间从 `1.0` 变到 `2.0`
- `resume` 后继续增长
- `stop` 后进入 `stopped`

所以这一块现在已经不是“计划中的能力”，而是实际可用能力。

## 43. 这一轮补了更宽松的聊天编辑输入

现在除了原来的固定句式，还额外支持这些说法：

- `增加流量`
- `减少流量`
- `把流量调大一点`
- `把流量调小一点`
- `车道改为5`
- `车道数改为5`

其中目前的默认处理是：

- `增加流量` / `把流量调大一点`：按 `+20%` 处理
- `减少流量` / `把流量调小一点`：按 `-20%` 处理

## 44. 现在如果只说“修改一下流量”这种模糊句子，不会再静默没反应

这类句子现在会明确提示：

- 已识别为修改请求
- 但没有识别到具体参数
- 并给出可用示例

这比之前“看起来像没反应”更适合实际使用和排查。
